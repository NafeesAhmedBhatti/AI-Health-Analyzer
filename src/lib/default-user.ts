/**
 * Default user constants for fallback scenarios.
 * Authentication is handled via NextAuth sessions — these are only used
 * as a last resort when no session exists but a single-user system is detected.
 */

export const DEFAULT_USER_ID = 'cmplh2vxg0000vxt6om0nh1vj';
export const DEFAULT_USER_NAME = 'Health User';
export const DEFAULT_USER_EMAIL = 'test@test.com';